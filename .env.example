# Frontend calls your secure proxy; do not expose keys in the browser
VITE_GEMINI_PROXY_URL=https://api.example.com/gemini

# Server-side only (proxy/Lambda). Do NOT place this in .env.local for production builds.
# GEMINI_API_KEY=your-gemini-key
# GEMINI_MODEL=gemini-2.5-flash
